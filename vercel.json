{
  "framework": "vite",
  "buildCommand": "npm run prebuild && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
